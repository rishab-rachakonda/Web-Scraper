httpx[http2]>=0.27.0
playwright>=1.44.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
rich>=13.7.0
typer>=0.12.0
pydantic>=2.7.0
pyyaml>=6.0.1
aiofiles>=23.2.1
aiosqlite>=0.20.0
apscheduler>=3.10.4
fake-useragent>=1.5.1
jmespath>=1.0.1

# Export formats
openpyxl>=3.1.0          # Excel (.xlsx)
fpdf2>=2.7.0             # PDF reports

# Anti-detection / TLS fingerprinting
curl_cffi>=0.7.0

# Browser stealth (optional — manual patches still apply without it)
playwright-stealth>=1.0.0

# Browser cookie import (optional)
browser-cookie3>=0.19.0
